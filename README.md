# yt-dlp TUI

Simple PowerShell app for Windows Terminal that wraps `yt-dlp.exe` with a small interactive menu.

## Run

```powershell
pwsh -ExecutionPolicy Bypass -File .\yt-dlp-tui.ps1
```

`yt-dlp.exe` must be available in PATH.